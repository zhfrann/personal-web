services:
    web:
        # build: .
        # image: personal-web:local
        image: ghcr.io/zhfrann/personal-web:latest
        container_name: personal-web
        restart: unless-stopped
        ports:
            - "127.0.0.1:8080:80"
